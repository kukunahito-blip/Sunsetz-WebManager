{
  "name": "sunsetz-webmanager",
  "version": "1.2.103",
  "description": "Adaptation of Sunsetz filebrowser in an app",
  "license": "ISC",
  "author": "Hitokuna <kukunahito@gmail.com>",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electronmon .",
    "dist": "electron-builder",
    "publish:patch": "git add . && npm version patch --force -m \"Build: patch %s\" && git push origin main --follow-tags",
    "publish:minor": "git add . && npm version minor --force -m \"Build: minor %s\" && git push origin main --follow-tags",
    "publish:major": "git add . && npm version major --force -m \"Build: major %s\" && git push origin main --follow-tags"
  },
  "build": {
    "appId": "com.sunsetz.webmanager",
    "productName": "WebManager-App",
    "files": [
      "**/*",
      "!build/*"
    ],
    "asar": true,
    "publish": [
      {
        "provider": "github",
        "owner": "kukunahito-blip",
        "repo": "Sunsetz-WebManager",
        "releaseType": "draft"
      }
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "assets/icon.ico"
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.icns"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "maintainer": "kukunahito@gmail.com",
      "icon": "assets/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "directories": {
      "buildResources": "assets",
      "output": "dist"
    }
  },
  "devDependencies": {
    "electron": "^40.0.0",
    "electron-builder": "^26.4.0"
  },
  "dependencies": {
    "electron-log": "^5.4.3",
    "electron-updater": "^6.7.3",
    "electron-version": "^2.0.1",
    "electronmon": "^2.0.4"
  }
}
